#include <iostream>
using namespace std;
int main ()
{int x;
cin>>x;
if(x>0)
cout<<"положительное";
else if (x<0)
cout<<"отрицательное";
else if (x==0)
cout<<"нулевое число";
if(x%2==0 && (x>0 || x<0))
cout<<" четное";
else if (x%2!=0 && (x>0 || x<0))
cout<<" нечетное";
return 0;
}
