#include <iostream>
#include <cmath>
using namespace std;
int main ()
{double x,y;
cin>>x>>y;
if(x>0 && y>0)
cout<<"1 четверть";
else if(x<0 && y>0)
cout<<"2 четверть";
else if(x<0 && y<0)
cout<<"3 четверть";
else if(x>0 && y<0)
cout<<"4 четверть";
return 0;
}
